#include <bits/stdc++.h>
using namespace std;

#ifdef DEBUG
#include "lib/debug.h"
#else
#define debug(...)
#endif

int32_t main() {
  ios::sync_with_stdio(false), cin.tie(0);

  auto solve = [&]() {
    int h, w, n;
    cin >> h >> w >> n;
    for (int i = 0; i < n; ++i) {
      int c, d;
      cin >> c >> d;
    }
  };

  {
    int tt = 1;
    while (tt--) solve();
  }
  return 0;
}
