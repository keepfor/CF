#include<bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include"lib/debug.h"
#else
#define debug(...) 
#endif

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int TESTCASES = 1;
  cin >> TESTCASES;
  while (TESTCASES--) {
    const int n = 1e3;
    for (int i = 0; i < n; ++i) cout << i;
    for (int i = 0; i < n; ++i) cout << i;
    for (int i = 0; i < n; ++i) cout << i;
  }
  return 0;
}
