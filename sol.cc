#include <bits/stdc++.h>

using ll = long long;

namespace sol {
using namespace std;

#ifdef DEBUG
#include "lib/debug.h"
#else
#define debug(...)
#endif

void test() {
  string x = to_string(111);
  cerr << x << '\n';
  ;x

'

}
}  // namespace sol

void Solve() { sol::test(); }

void Main() {
  auto t{1};
  // cin >> t;
  while (t--) {
    Solve();
  }
}

using namespace std;
inline void SetIO() { cin.tie(0)->sync_with_stdio(0); }

signed main() {
  SetIO();
  Main();
  return 0;
}
