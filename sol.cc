#include<bits/stdc++.h>

#ifdef LOCAL
#include"debug.h"
#else 
#define debug(...) 0
#endif

using namespace std;

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  function<void()> f;
  auto cmp = []() {
  };
  set<int, decltype(cmp)> s(cmp);
  priority_queue<int, vector<int>, decltype(cmp)> p(cmp);
  
  return 0;
}
