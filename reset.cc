#include <bits/stdc++.h>

using namespace std;

#ifdef DEBUG
#include "lib/debug.cc"
#else
#define debug(...) 0
#endif

int32_t main() {
  auto SetIO = []() -> void { cin.tie(0)->sync_with_stdio(0); };
  SetIO();
  auto SolveOne = []() -> void {
    // TODO
  };
  auto Solve = [&SolveOne]() -> void {
    int t = 1;
    while (t--) {
      SolveOne();
    }
  };
  Solve();
  return 0;
}
