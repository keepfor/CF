#ifndef CP
#define CP
#include<bits/stdc++.h>
using namespace std;
#endif

#ifdef LOCAL
#include"./lib/debug.h"
#else 
#define debug(...) 0
#endif

#include "./atcoder/dsu.hpp"

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int tt = 1;
  atcoder::dsu d(15);
  cin >> tt;
  while (tt--) {
  }
  return 0;
}
