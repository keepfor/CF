#include <bits/stdc++.h>

namespace solution {
using namespace std;

#ifdef DEBUG
#include "lib/debug.h"
#else
#define debug(...)
#endif

using ll = long long;

void Solve() {}

void Main() {
  auto t{1};
  // cin >> t;
  while (t--) {
    Solve();
  }
}

inline void SetIO() { cin.tie(0)->sync_with_stdio(0); }

void main() {
  SetIO();
  Main();
}
}  // namespace solution

signed main() {
  solution::main();
  return 0;
}